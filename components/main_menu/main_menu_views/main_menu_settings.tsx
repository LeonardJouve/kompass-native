import React from 'react';
import {Text} from '@renative';

const MainMenuSettings = () => {
    return (
        <Text variants={['primary']}>{'settings'}</Text>
    );
};

export default MainMenuSettings;
