import React from 'react';
import {Text} from '@renative/index';

const MainMenuWeather = () => {
    return (
        <Text variants={['primary']}>{'weather'}</Text>
    );
};

export default MainMenuWeather;
