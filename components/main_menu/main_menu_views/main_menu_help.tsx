import React from 'react';
import {Text} from '@renative/index';

const MainMenuHelp = () => {
    return (
        <Text variants={['primary']}>{'help'}</Text>
    );
};

export default MainMenuHelp;
